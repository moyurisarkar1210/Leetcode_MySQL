SELECT DISTINCT activity_date AS day, COUNT(DISTINCT user_id) AS active_users
From Activity
GROUP BY activity_date
HAVING COUNT(DISTINCT user_id)>=1 AND (activity_date BETWEEN DATE_SUB('2019-07-27', INTERVAL 29 DAY) AND '2019-07-27')